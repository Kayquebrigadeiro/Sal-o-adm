# Database (TiDB / MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=nome_do_banco

# JWT
JWT_SECRET=troque_por_um_seguro_segredo

# Server
PORT=3333

# CORS (production)
# Multiple origins separated by comma
ALLOWED_ORIGINS=http://localhost:5173,https://seu-dominio.vercel.app
